---
import BaseLayout from '../layouts/BaseLayout.astro';
import Eyebrow from '../components/Eyebrow.astro';

// The BOIP / merkregistratie card was removed: it claimed a registered
// Benelux trademark with specific classes and a pending deposit number. That
// is a legal claim, and it should only appear here once the registration
// actually exists. What remains is true by default -- copyright arises
// automatically on creation, and licensing is an offer rather than a claim.
const feiten = [
  {
    label: 'Auteursrecht',
    waarde: 'Van rechtswege',
    note: 'Teksten en beeldend werk zijn beschermd vanaf het moment van creatie. Registratie is geen voorwaarde.',
  },
  {
    label: 'Licenties',
    waarde: 'Op aanvraag',
    note: 'Gebruik in publicaties, tentoonstellingen of onderwijs kan worden gelicentieerd via een schriftelijke overeenkomst.',
  },
];

const wel = [
  'Delen van een pagina via een link naar deze site.',
  'Kort citeren met bronvermelding, volgens het citaatrecht.',
  'Persgebruik van aangeleverd beeldmateriaal met credit.',
];

const niet = [
  'Reproductie of verkoop van werk zonder licentie.',
  'Bewerken, natekenen of hergebruiken als basis voor nieuw werk.',
  'Gebruik van teksten of beelden als trainingsdata voor AI-modellen.',
];
---

<BaseLayout
  title="Auteursrechten — NIBF"
  path="/auteursrechten"
  description="Wat wel en niet mag met het werk op deze site, en hoe je een licentie aanvraagt."
>
  <section class="section--page">
    <Eyebrow text="02 — Auteursrechten" />
    <h1 class="h1 h1--page">Auteursrecht op dit werk</h1>
    <p class="page-intro">
      Al het werk op deze site — teksten, beelden en de vormgeving ervan — is intellectueel
      eigendom van Nisrine Bolakhrif. Hieronder staat wat wel en niet mag zonder toestemming.
    </p>

    <div class="factgrid">
      {
        feiten.map((f) => (
          <div class="fact">
            <div class="fact__label">{f.label}</div>
            <div class="fact__value">{f.waarde}</div>
            <div class="fact__note">{f.note}</div>
          </div>
        ))
      }
    </div>

    <div data-terms-grid>
      <div>
        <h2 class="terms__h">Wel toegestaan</h2>
        <ul class="terms">
          {wel.map((t) => <li><span class="terms__yes">+</span> {t}</li>)}
        </ul>
      </div>
      <div>
        <h2 class="terms__h">Niet toegestaan</h2>
        <ul class="terms">
          {niet.map((t) => <li><span class="terms__no">–</span> {t}</li>)}
        </ul>
      </div>
    </div>

    <div class="callout">
      <p>
        Vragen over gebruik of een licentieverzoek? Stuur een bericht via{' '}
        <a href="/contact">contact</a> met een korte beschrijving van het beoogde gebruik, de
        oplage en de periode.
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  h1 {
    max-width: 20ch;
    margin-bottom: 24px;
  }

  .page-intro {
    max-width: 62ch;
    margin: 0 0 52px;
  }

  [data-terms-grid] {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 56px;
  }
</style>
